<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      <!--wrapper-->
      <div class="container">
          <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <%= link_to 'Navbar', articles_path, class: "navbar-brand"%>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active">
                <%= link_to 'New article', new_article_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to 'About', about_path, class: 'nav-link' %>
              </li>
              <li class="nav-item">
                <%= link_to 'Terms', terms_path, class: 'nav-link' %>
              </li>
            </ul>
            <% if user_signed_in? %>
                <span style='margin-right: 10px;'><%= current_user.email %></span>
                <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn btn-dark' %>
            <% else %>
                <%= link_to 'Log in', new_user_session_path, class: 'btn btn-dark', style: 'margin-right: 10px;' %> <%= link_to 'Sing up', new_user_registration_path, class: 'btn btn-dark' %>
            <% end %>
            </div>
          </nav>
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
          <%= yield %>
      </div>
  </body>
</html>
